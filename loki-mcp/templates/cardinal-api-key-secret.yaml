{{- if .Values.cardinal.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "loki-mcp.cardinalSecretName" . }}
  labels:
    {{- include "loki-mcp.labels" . | nindent 4 }}
type: Opaque
stringData:
  CARDINALHQ_API_KEY: {{ .Values.cardinal.apiKey | quote }}
{{- end }}
