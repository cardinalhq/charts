apiVersion: v2
name: conductor
description: Unified Helm chart for CardinalHQ LakeRunner + Maestro
type: application
version: "0.6.0"
# appVersion is nominal only — component image tags are pinned per-component in values.yaml
# (lakerunner v1.90.0 / maestro v1.96.2 as of this release). Both lakerunner.* and
# maestro.* image helpers would otherwise default the tag to this.
appVersion: "v1.96.2"
keywords:
  - lakerunner
  - maestro
  - logs
  - metrics
  - traces
  - observability
home: https://docs.cardinalhq.io/lakerunner
maintainers:
  - name: Cardinal HQ
    email: support@cardinalhq.com
