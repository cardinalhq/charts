{{- if .Values.conductorServer.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "conductor-server.fullname" . }}-conductor-server
  labels:
    {{- include "conductor-server.labels" . | nindent 4 }}
    app.kubernetes.io/component: conductor-server
  {{- include "conductor-server.annotations" . | nindent 2 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.conductorServer.port }}
      targetPort: {{ .Values.conductorServer.port }}
      name: http
  selector:
    {{- include "conductor-server.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: conductor-server
{{- end }}
