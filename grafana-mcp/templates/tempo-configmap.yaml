apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "grafana-mcp.fullname" . }}-tempo
  labels:
    {{- include "grafana-mcp.labels" . | nindent 4 }}
data:
  tenants.yaml: |
    tenants:
{{- range $tenantName, $headers := .Values.tempo.tenants }}
      {{ $tenantName }}:
{{- range $headerKey, $headerValue := $headers }}
        {{ $headerKey }}: {{ $headerValue | quote }}
{{- end }}
{{- end }}
