{{- if and .Values.boxerRollupMetrics.enabled (eq (include "lakerunner.autoscalingMode" (list .Values.boxerRollupMetrics.autoscaling .)) "keda") -}}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "lakerunner.fullname" . }}-boxer-rollup-metrics
  labels:
    {{- include "lakerunner.labels" . | nindent 4 }}
    app.kubernetes.io/component: boxer-rollup-metrics
  {{ include "lakerunner.annotations" . | nindent 2 }}
spec:
  scaleTargetRef:
    name: {{ include "lakerunner.fullname" . }}-boxer-rollup-metrics
  pollingInterval: {{ .Values.boxerRollupMetrics.autoscaling.keda.pollingInterval }}
  cooldownPeriod: {{ .Values.boxerRollupMetrics.autoscaling.keda.cooldownPeriod }}
  minReplicaCount: {{ .Values.boxerRollupMetrics.autoscaling.minReplicas }}
  maxReplicaCount: {{ .Values.boxerRollupMetrics.autoscaling.maxReplicas }}
  triggers:
  - type: external
    metadata:
      scalerAddress: "{{ include "lakerunner.fullname" . }}-monitoring.{{ .Release.Namespace }}.svc.cluster.local:9090"
      serviceType: "boxer-rollup-metrics"
{{- end }}