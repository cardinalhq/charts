{{- if and .Values.rollupMetrics.enabled (eq (include "lakerunner.autoscalingMode" (list .Values.rollupMetrics.autoscaling .)) "hpa") -}}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "lakerunner.fullname" . }}-rollup-metrics
  labels:
    {{- include "lakerunner.labels" . | nindent 4 }}
    app.kubernetes.io/component: rollup-metrics
  {{ include "lakerunner.annotations" . | nindent 2 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "lakerunner.fullname" . }}-rollup-metrics
  minReplicas: {{ .Values.rollupMetrics.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.rollupMetrics.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.rollupMetrics.autoscaling.hpa.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.rollupMetrics.autoscaling.hpa.targetCPUUtilizationPercentage }}
  {{- end }}
  behavior:
    scaleDown:
      policies:
      - type: Pods
        value: 4
        periodSeconds: 60
{{- end }}
