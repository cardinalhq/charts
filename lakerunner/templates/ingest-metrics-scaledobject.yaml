{{- if and .Values.ingestMetrics.enabled (eq (include "lakerunner.autoscalingMode" (list .Values.ingestMetrics.autoscaling .)) "keda") -}}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "lakerunner.fullname" . }}-ingest-metrics
  labels:
    {{- include "lakerunner.labels" . | nindent 4 }}
    app.kubernetes.io/component: ingest-metrics
  {{ include "lakerunner.annotations" . | nindent 2 }}
spec:
  scaleTargetRef:
    name: {{ include "lakerunner.fullname" . }}-ingest-metrics
  pollingInterval: {{ .Values.ingestMetrics.autoscaling.keda.pollingInterval }}
  cooldownPeriod: {{ .Values.ingestMetrics.autoscaling.keda.cooldownPeriod }}
  minReplicaCount: {{ .Values.ingestMetrics.autoscaling.minReplicas }}
  maxReplicaCount: {{ .Values.ingestMetrics.autoscaling.maxReplicas }}
  triggers:
  - type: postgresql
    metadata:
      host: {{ .Values.database.lrdb.host }}
      port: {{ .Values.database.lrdb.port | quote }}
      dbName: {{ .Values.database.lrdb.name }}
      userName: {{ .Values.database.lrdb.username }}
      sslmode: {{ .Values.database.lrdb.sslMode | default "require" }}
      query: "SELECT COALESCE(COUNT(*), 0) FROM inqueue WHERE telemetry_type='metrics' AND claimed_at IS NULL"
      targetQueryValue: {{ .Values.ingestMetrics.autoscaling.keda.targetQueueDepth | quote }}
      activationTargetQueryValue: {{ .Values.ingestMetrics.autoscaling.keda.activationQueueDepth | quote }}
      metricType: "Value"
    authenticationRef:
      name: {{ include "lakerunner.fullname" . }}-keda-auth
{{- end }}
