{{- if and .Values.ingestTraces.enabled (eq (include "lakerunner.autoscalingMode" (list .Values.ingestTraces.autoscaling .)) "hpa") -}}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "lakerunner.fullname" . }}-ingest-traces
  labels:
    {{- include "lakerunner.labels" . | nindent 4 }}
    app.kubernetes.io/component: ingest-traces
  {{ include "lakerunner.annotations" . | nindent 2 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "lakerunner.fullname" . }}-ingest-traces
  minReplicas: {{ .Values.ingestTraces.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.ingestTraces.autoscaling.maxReplicas }}
  traces:
  {{- if .Values.ingestTraces.autoscaling.hpa.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.ingestTraces.autoscaling.hpa.targetCPUUtilizationPercentage }}
  {{- end }}
  behavior:
    scaleDown:
      policies:
      - type: Pods
        value: 4
        periodSeconds: 60
{{- end }}
