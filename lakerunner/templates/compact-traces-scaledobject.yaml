{{- if and .Values.compactTraces.enabled (eq (include "lakerunner.autoscalingMode" (list .Values.compactTraces.autoscaling .)) "keda") -}}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "lakerunner.fullname" . }}-compact-traces
  labels:
    {{- include "lakerunner.labels" . | nindent 4 }}
    app.kubernetes.io/component: compact-traces
  {{ include "lakerunner.annotations" . | nindent 2 }}
spec:
  scaleTargetRef:
    name: {{ include "lakerunner.fullname" . }}-compact-traces
  pollingInterval: {{ .Values.compactTraces.autoscaling.keda.pollingInterval }}
  cooldownPeriod: {{ .Values.compactTraces.autoscaling.keda.cooldownPeriod }}
  minReplicaCount: {{ .Values.compactTraces.autoscaling.minReplicas }}
  maxReplicaCount: {{ .Values.compactTraces.autoscaling.maxReplicas }}
  triggers:
  - type: postgresql
    metadata:
      host: {{ .Values.database.lrdb.host }}
      port: {{ .Values.database.lrdb.port | quote }}
      dbName: {{ .Values.database.lrdb.name }}
      userName: {{ .Values.database.lrdb.username }}
      sslmode: {{ .Values.database.lrdb.sslMode | default "require" }}
      query: "SELECT COALESCE(COUNT(*), 0) FROM work_queue WHERE needs_run=true AND runnable_at <= now() AND signal='traces' AND action='compact'"
      targetQueryValue: {{ .Values.compactTraces.autoscaling.keda.targetQueueDepth | quote }}
      activationTargetQueryValue: {{ .Values.compactTraces.autoscaling.keda.activationQueueDepth | quote }}
      metricType: "Value"
    authenticationRef:
      name: {{ include "lakerunner.fullname" . }}-keda-auth
{{- end }}
