{{- if and .Values.compactTraces.enabled (eq (include "lakerunner.autoscalingMode" (list .Values.compactTraces.autoscaling .)) "keda") -}}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "lakerunner.fullname" . }}-compact-traces
  labels:
    {{- include "lakerunner.labels" . | nindent 4 }}
    app.kubernetes.io/component: compact-traces
  {{ include "lakerunner.annotations" . | nindent 2 }}
spec:
  scaleTargetRef:
    name: {{ include "lakerunner.fullname" . }}-compact-traces
  pollingInterval: {{ .Values.compactTraces.autoscaling.keda.pollingInterval }}
  cooldownPeriod: {{ .Values.compactTraces.autoscaling.keda.cooldownPeriod }}
  minReplicaCount: {{ .Values.compactTraces.autoscaling.minReplicas }}
  maxReplicaCount: {{ .Values.compactTraces.autoscaling.maxReplicas }}
  triggers:
  - type: external
    metadata:
      scalerAddress: "{{ include "lakerunner.fullname" . }}-monitoring.{{ .Release.Namespace }}.svc.cluster.local:9090"
      serviceType: "worker-compact-traces"
  {{- if .Values.compactTraces.autoscaling.keda.cpuTrigger.enabled }}
  - type: cpu
    metricType: Utilization
    metadata:
      value: "{{ .Values.compactTraces.autoscaling.keda.cpuTrigger.targetUtilization }}"
  {{- end }}
{{- end }}