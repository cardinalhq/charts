{{- if and .Values.perch.enabled .Values.perch.maestro.webhookSecret -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "lakerunner.fullname" . }}-perch
  labels:
    {{- include "lakerunner.labels" . | nindent 4 }}
    app.kubernetes.io/component: perch
  {{ include "lakerunner.annotations" . | nindent 2 }}
type: Opaque
data:
  webhook-secret: {{ .Values.perch.maestro.webhookSecret | b64enc | quote }}
{{- end }}
