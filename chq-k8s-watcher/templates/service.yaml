apiVersion: v1
kind: Service
metadata:
  name: {{ include "chq-k8s-watcher.fullname" . }}
  labels:
    {{- include "chq-k8s-watcher.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chq-k8s-watcher.selectorLabels" . | nindent 4 }}
